import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Resultados Comprobados | +25% Eficiencia con Merlina IA",
  description: "Empresas que usan Merlina logran +25% de eficiencia operativa, -30% de tiempo en análisis y +15% en oportunidades comerciales en 90 días.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
